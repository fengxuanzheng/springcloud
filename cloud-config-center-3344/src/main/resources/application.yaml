server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri:  https://gitee.com/fengxuanzheng/springcloud.git
          search-paths:
            - springcloud
          username: fengxuanzheng@sina.com
          password: feng820606
      label:  master

eureka:
  instance:
    instance-id: config
    prefer-ip-address: true
    lease-expiration-duration-in-seconds: 2
    lease-renewal-interval-in-seconds: 2
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7001/eureka,http://localhost:7002/eureka


